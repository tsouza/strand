# `unicode-rs/unicode-segmentation` — license and version

Vendored finding. Fetched 2026-08-18. Groundwork for the not-yet-drafted M1
analyzer-descriptor RFC (invariant 6) — `docs/research/README.md` R4 already names
this crate as a candidate tokenizer-implementation source; not yet cited by an
approved RFC.

**License:** dual Apache-2.0 / MIT — confirmed via the repository's own
`LICENSE-APACHE` and `LICENSE-MIT` files (`github.com/unicode-rs/unicode-
segmentation`), the standard Rust-ecosystem dual-license pattern. GitHub's license-
detection API reports `NOASSERTION` for this repository (it does not resolve
dual-license repos to a single SPDX identifier), which is why this was confirmed by
listing the actual license files present rather than trusting the API's single-field
summary. Both options are Apache-2.0-compatible.

**Version:** `1.13.3` (from the default branch's `Cargo.toml`, `gh api
repos/unicode-rs/unicode-segmentation/contents/Cargo.toml`). No GitHub Releases are
published for this repository (confirmed 404 on the releases API) — it is versioned
via crates.io/git tags only, so `Cargo.toml`'s own `version` field is the
authoritative source, not a release page.
